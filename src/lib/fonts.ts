import { Lato, Open_Sans, Merriweather, Noto_Sans } from 'next/font/google';

export const lato = Lato({
  weight: '400',
  style: 'normal',
  subsets: ['latin'],
});

export const openSans = Open_Sans({
  weight: '400',
  style: 'normal',
  subsets: ['latin'],
});

export const merriweather = Merriweather({
  weight: '400',
  style: 'normal',
  subsets: ['latin'],
});

export const notoSans = Noto_Sans({
  weight: '400',
  style: 'normal',
  subsets: ['latin'],
});
